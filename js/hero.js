/* ══ KIVO hero.js — DESACTIVADO (23-07-2026) ══
   El ciclo de color global (--cycle-color) ahora se hace 100% en CSS con una
   animación de crossfade suave (@keyframes kivoCycleGlobal en index.html),
   con verde inglés KIVO (#01462E), naranja, dorado y turquesa. SIN azul marino.
   Este archivo ya no rota colores para no pisar la animación CSS. */
(function(){ /* no-op */ })();
